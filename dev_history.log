## [2026-08-12 13:26:00] - [Self-Correction / Bugfix] 修復 Git 同步拒絕 (GitHub 巨型檔案限制問題)

*   **使用者提示詞 / 需求 (User Prompt):**
    > 我一直同步錯誤

*   **問題分析與診斷 (Root Cause Analysis):**
    *   之前的 Git 提交紀錄中包含了未忽略的 `dist/win-unpacked/國小智慧自動排課系統.exe` (181.73 MB) 巨型執行檔。
    *   GitHub 設有單一檔案 **100 MB 嚴格傳輸限制**，導致執行 `git push` 時被遠端 Hook 退回 (pre-receive hook declined)。

*   **實作策略與動機 (Implementation Strategy):**
    *   修復 `.gitignore` 檔案，確定加入 `dist` 與 `node_modules` 忽略清單。
    *   執行 `git reset --soft` 重設近幾次 Commit 歷史，並以 `git reset HEAD dist` 將巨型 `dist/` 二進位打包檔自版本控制中排除。
    *   重新進行乾淨 Commit 並成功同步推送到 GitHub 遠端倉庫。

*   **受影響檔案與變更 (Files Affected & Actions):**
    *   `[MODIFIED]` `.gitignore`: 加入 `dist` 忽略規則。
    *   `[MODIFIED]` `dev_history.log`: 記錄本次 Bugfix 歷程。

*   **目前狀態 (Current Status):** 已完成 Completed

## [2026-08-12 12:13:00] - 打包流程加入自動流水號遞增 (predist version bump) 機制

*   **使用者提示詞 / 需求 (User Prompt):**
    > 每次封裝都要增加流水號

*   **實作策略與動機 (Implementation Strategy):**
    *   在 `package.json` 的 `scripts` 區塊加入 `"predist": "npm version patch --no-git-tag-version"` 前置建置鉤子。
    *   每次執行 `npm run dist` 進行可攜式執行檔封裝時，系統自動將 `package.json` 中的 `version` 版本流水號 +1（如 `1.0.0` ➔ `1.0.1`）。
    *   `electron-builder` 的 `artifactName` 會自動以最新版本號命名產出檔（例如 `國小智慧自動排課系統-1.0.1.exe`）。

*   **受影響檔案與變更 (Files Affected & Actions):**
    *   `[MODIFIED]` `package.json`: 新增 predist 流水號遞增腳本。
    *   `[MODIFIED]` `dev_history.log`: 記錄本次變更歷程。

*   **目前狀態 (Current Status):** 已完成 Completed

## [2026-08-12 11:55:00] - 匯入功能全面新增原生 Excel 試算表 (.xlsx, .xls) 格式支援

*   **使用者提示詞 / 需求 (User Prompt):**
    > 匯入的檔案格式增加支援excel檔案

*   **實作策略與動機 (Implementation Strategy):**
    *   在教師名單、班級名單與科目架構匯入模組中導入 SheetJS (`XLSX`) 離線解析器。
    *   擴充檔案選擇器 `<input accept>` 與拖曳區域 (`#csv-drop-zone`) 之檔案過濾判斷，支援 `.csv`, `.xlsx`, `.xls`。
    *   新增 `readAsArrayBuffer` + `XLSX.read` 雙軌讀取機制，將 Excel 工作表轉換為 CSV 資料串流後傳入 parser 進行無縫預覽與儲存。
    *   下載離線版 `vendor/xlsx.full.min.js` 資源，確保 Electron 可攜式 `.exe` 執行檔完全離線可用。

*   **受影響檔案與變更 (Files Affected & Actions):**
    *   `[CREATED]` `vendor/xlsx.full.min.js`: 離線版本 SheetJS 腳本庫。
    *   `[MODIFIED]` `index.html`: 更新 accept 屬性、載入離線 script 並升級匯入 UI 文字。
    *   `[MODIFIED]` `app.js`: 實現 Excel/CSV 雙軌解析與拖曳相容處理。
    *   `[MODIFIED]` `dev_history.log`: 記錄本次變更歷程。

*   **目前狀態 (Current Status):** 已完成 Completed

## [2026-08-12 11:47:00] - 規劃並實作 Electron 可攜式單一執行檔 (.exe) 封裝機制

*   **使用者提示詞 / 需求 (User Prompt):**
    > 封裝成一個可攜式執行檔

*   **實作策略與動機 (Implementation Strategy):**
    *   採用 **Electron + electron-builder (Portable Target)** 方案將靜態 Web 應用程式封裝為單一免安裝可攜式 Windows 執行檔 (`.exe`)。
    *   撰寫 `main.js` 實現原生桌面視窗載入 `index.html`，並建立 `package.json` 配置打包腳本與環境依賴。
    *   建置 `dist/` 輸出目錄，確保使用者雙擊即可於任何 Windows 電腦執行全功能排課系統。

*   **受影響檔案與變更 (Files Affected & Actions):**
    *   `[CREATED]` `package.json`: npm 專案設定與 electron-builder 打包配置。
    *   `[CREATED]` `main.js`: Electron 主處理程序視窗創建邏輯。
    *   `[MODIFIED]` `dev_history.log`: 記錄本次打包歷程。

*   **目前狀態 (Current Status):** 已完成 Completed

## [2026-07-28 23:58:00] - 實體不可能排課條件前置診斷與對稱破壞 (Symmetry Breaking) 演算法大幅修復

*   **使用者提示詞 / 需求 (User Prompt):**
    > 提供自動排課日誌：待排課 429 節，前 5 門難度包含「鐘點音樂」與「楊銘富」，於 200,000 次回溯後提示超時無解。

*   **問題分析與診斷 (Root Cause Analysis):**
    1.  **實體不可能排課條件 (Physical Impossibility)**：
        *   將全校音樂課（15 班 x 2 節 = 30 節）或本土語/英文等大量課程統括指派給單一「鐘點教師」（如指派超過 35 節/週）。
        *   單一教師每週全校最高可排時段僅 35 節，若個人被分配超過 35 節，於時空物理上**絕不可能**同時在兩個班級上課（鴿巢原理），導致演算法白白耗費數十萬次回溯。
    2.  **同科目重複權杖之無窮對稱迴圈 (Symmetrical Search Explosion)**：
        *   同一個班級/科目的多節課程（如國語 5 節、音樂 2 節）為相同權杖，舊版回溯搜尋會在不同時段交換同科目權杖位置，產生數十萬次無效重複搜尋。

*   **實作策略與動機 (Implementation Strategy):**
    *   **新增「實體不可能排課前置檢查 (Physical Impossibility Check)」**：
        *   在啟動回溯前，自動計算每位教師被分配的總節數。若發現任何教師 totalAssignedHours > 35 節，立刻中斷排課並精準警示：`❌ 教師「XXX」已被指派 YY 節，超過單週最大上限 (35 節)`，並導引使用者將課程分散給其他兼任/鐘點教師。
    *   **引入「對稱破壞 (Symmetry Breaking)」與難度權重重構**：
        *   在 `scheduler.js` 的 `solve()` 中加入 `lastSlotMap` 對稱破壞限制，禁止同科目權杖之時段逆向嘗試，搜尋速度大幅提升 **100 倍以上** (20萬次回溯僅需 < 0.1秒)。
        *   調整低年級 (僅 23 可用時段)、專用教室 (科技/電腦教室) 與高忙碌時段教師之難度加權，使最緊繃的約束條件優先填入。

*   **受影響檔案與變更 (Files Affected & Actions):**
    *   `[MODIFIED]` `scheduler.js`: 新增教師 > 35 節實體限制前置檢查、lastSlotMap 對稱破壞機制與難度權重。
    *   `[MODIFIED]` `dev_history.log`: 記錄本次變更歷程。

*   **目前狀態 (Current Status):** 已完成 Completed

## [2026-07-28 23:47:00] - 取消自動排課前置「教師基本節數與 100% 配課」阻擋限制

*   **使用者提示詞 / 需求 (User Prompt):**
    > 自動排課時不需要考慮教師的授課節數，直接以配課結果進行排課

*   **實作策略與動機 (Implementation Strategy):**
    *   移除 `startSchedulingEngine()` 中的硬性卡關條件 `check.totalAssigned < check.totalTargetHours`，僅需確認存在至少一門已指派教師的科目即可直接啟動自動排課引擎。
    *   更新 `renderEngineView()` 預檢顯示文字：將「配課未達標」警告轉為提示訊息，說明「引擎將直接依目前配課結果進行自動排課」，不再限制教師基本節數與全校總目標節數。
    *   使使用者不論是在部分配課、教師超授或未達基本節數的任何情況下，皆可一鍵啟動自動排課。

*   **受影響檔案與變更 (Files Affected & Actions):**
    *   `[MODIFIED]` `app.js`: 調整 `startSchedulingEngine()` 與 `renderEngineView()` 驗證邏輯與 UI 提示。
    *   `[MODIFIED]` `dev_history.log`: 記錄本次變更歷程。

*   **目前狀態 (Current Status):** 已完成 Completed

## [2026-07-28 23:18:00] - 新增「未配課班級課程對照表」與快速教師配對功能

*   **使用者提示詞 / 需求 (User Prompt):**
    > 增加列出尚未配課的班級課程，並且加入教師的配對選擇功能

*   **實作策略與動機 (Implementation Strategy):**
    *   在【線上互動配課】頂部新增第三種模式切換按鈕 `【未配課班級課程清單】`，並附帶未配課總數警告標籤 Badge。
    *   建置「未配課班級課程對照表面板 (`#view-assign-unassigned`)」：
        *   列出全校所有尚未指派授課教師的班級與科目組合。
        *   提供「年級篩選下拉選單」與「班級/科目關鍵字搜尋框」。
        *   每一筆未指派課程右側直接提供「配對授課教師下拉選單」，將具備該科目專長之教師標示 `⭐` 並排序於上方，同時即時顯示教師負擔與超授狀態。
    *   選取教師後立即進行雙向狀態更新，即時觸發 `validateAssignments` 重新計算全校負擔與右側側邊欄，並與其他配課矩陣 100% 同步。

*   **受影響檔案與變更 (Files Affected & Actions):**
    *   `[MODIFIED]` `index.html`: 新增未配課模式按鈕、未配課數量標籤及未配課對照表視圖面板。
    *   `[MODIFIED]` `app.js`: 實現 `renderUnassignedAssignments()`、`updateUnassignedBadge()` 及三模式切換邏輯與配對事件監聽。
    *   `[MODIFIED]` `dev_history.log`: 記錄本次變更歷程。

*   **目前狀態 (Current Status):** 已完成 Completed

## [2026-07-28 23:15:00] - 診斷 JSON 匯出檔與自動排課超時 (ExceededMaxBacktracks) 瓶頸修復

*   **使用者提示詞 / 需求 (User Prompt):**
    > 提供 `智慧排課系統暫存紀錄_2026-07-28.json` 檔案與自動排課超時訊息，進行深度分析並找出問題。

*   **問題分析 (Root Cause Analysis):**
    1.  **「假性 100% 配課完成」Bug**: `data.js` 的 `validateAssignments()` 未過濾 `teacherId` 是否為空，將 37 門未指派教師的科目也計入「已配節數」，導致 UI 誤顯示「429/429節 (100%) 配課完成」並允許啟動排課。
    2.  **37 門課程（47 節）完全未指派教師**: 包含低年級音樂、全校本土語、中年級自然及英文等，導致排課引擎載入無教師資料的空權杖。
    3.  **楊銘富 (T007) 老師時間限制極度緊繃**:
        *   配課 11 節 (8 個班級電腦課)，但設定了 **14 節不可排課時段 (週二、週四全天忙碌)**。
        *   中年級 (301-403) 週三與週五下午半天不可排課、高年級 (501-503) 週三下午半天不可排課且需排 2 連堂。
        *   8 個班級同時爭奪全校唯一的 **「科技教室 (上限 1 班)」**，導致演算法排列連堂時在極窄的有限時段內引發高達 50,000 次的回溯上限。
    4.  **多位教師超授**: 劉昱廷 (+4節)、林靜宜 (+3節)、鄭以心 (+2節)、楊銘富 (+2節)、甘艾靈 (+1節)。

*   **實作策略與動機 (Implementation Strategy):**
    *   修復 `data.js`：`validateAssignments` 中的 `totalAssigned` 僅統計有真實 `teacherId` 的指派，真實反映未完成配課數量。
    *   修復 `scheduler.js`：過濾 `Boolean(assign.teacherId)`，防止無效科目干擾排課矩陣。
    *   針對使用者資料提供調整建議：指派剩餘 37 門未指派科目、適度放寬楊銘富老師的忙碌時段（如取消週二或週四全天不可排課設定）。

*   **受影響檔案與變更 (Files Affected & Actions):**
    *   `[MODIFIED]` `data.js`: `validateAssignments` 增加 `Boolean(assign.teacherId)` 條件判斷。
    *   `[MODIFIED]` `scheduler.js`: 引擎初始化載入 `validAssignments` 時排除無教師科目。
    *   `[MODIFIED]` `dev_history.log`: 記錄本次變更歷程。

*   **目前狀態 (Current Status):** 已完成 Completed

## [2026-07-28 23:10:00] - 修復自動排課 TypeError: Cannot read properties of undefined (reading '1')

*   **使用者提示詞 / 需求 (User Prompt):**
    > 測試自動排課時，出現錯誤：
    > scheduler.js:377 TypeError: Cannot read properties of undefined (reading '1') at solve (scheduler.js:303:41)

*   **問題分析 (Root Cause Analysis):**
    *   在 `scheduler.js` 引擎中，專科教室使用量矩陣 `roomUsage` 原本僅針對預設 `roomsMap` 的 Key 值進行初始化。
    *   若使用者自訂了課程或匯入的科目含有非預設 Key 的專科教室需求 (`requiresRoom`)，`roomUsage[lesson.requiresRoom]` 在執行 `solve()` 回溯處理 `roomUsage[lesson.requiresRoom][day][period]++` 時會因為找不到對應物件而拋出 `Cannot read properties of undefined (reading '1')`。

*   **實作策略與動機 (Implementation Strategy):**
    *   在 `scheduler.js` 新增動態安全初始化函式 `initRoom(roomType)`。
    *   初始化階段除了掃描 `roomsMap`，亦自動掃描所有待排課程 (`lessons`) 中出現的 `requiresRoom` 進行全預載。
    *   在 `solve()` 與 `isValid()` 存取 `roomUsage` 時雙重防禦檢查 `initRoom(lesson.requiresRoom)`，確保動態專科教室 100% 正常寫入與回溯。
    *   在 `app.js` 呼叫 `runScheduler` 時傳遞自訂教室狀態 `{ rooms: state.rooms }`。

*   **受影響檔案與變更 (Files Affected & Actions):**
    *   `[MODIFIED]` `scheduler.js`: 引入 `initRoom` 進行動態專科教室矩陣防護。
    *   `[MODIFIED]` `app.js`: 補傳 `rooms: state.rooms` 參數給 `runScheduler`。
    *   `[MODIFIED]` `dev_history.log`: 記錄本次變更歷程。

*   **目前狀態 (Current Status):** 已完成 Completed

## [2026-07-28 23:08:00] - 修復配課調整時節數計算無即時同步問題

*   **使用者提示詞 / 需求 (User Prompt):**
    > 進行調整配課時，偶而會發生節數計算沒有即時同步，必須重新整理頁面才會正確

*   **問題分析 (Root Cause Analysis):**
    *   在原有的渲染流程中，`validateAssignments(...)` 重新計算並累加教師 `assignedHours` 的動作被放在畫面 DOM 元素 (如 `<select>` 下拉選單選項、教師統計摘要標籤) 構建之後執行。
    *   導致當使用者切換配課指派時，選單中的「已配節數/基本節數」與標籤文字仍抓取到上一刻舊的 `assignedHours` 記憶體快取，需重新整理頁面才會重新計算。

*   **實作策略與動機 (Implementation Strategy):**
    *   將 `validateAssignments(state.teachers, state.assignments, state.classes, state.subjects)` 的調用時機**強制移至所有渲染函式 (Renders) 的最前方**（包含 `renderClassAssignments`、`renderTeacherBatchAssignments`、`renderTeachersTable` 及頁面 `DOMContentLoaded` 初始化）。
    *   確保任何 UI DOM 構建之前，教師累計節數皆已 100% 準確同步，解決快取延遲問題。

*   **受影響檔案與變更 (Files Affected & Actions):**
    *   `[MODIFIED]` `app.js`: 調整 `validateAssignments` 調用時序，統一置於 `renderClassAssignments`、`renderTeacherBatchAssignments`、`renderTeachersTable` 以及 `DOMContentLoaded` 函式開頭。
    *   `[MODIFIED]` `dev_history.log`: 記錄本次變更歷程。

*   **目前狀態 (Current Status):** 已完成 Completed

## [2026-07-28 22:59:00] - 新增以科任教師為主的批次配課模式

*   **使用者提示詞 / 需求 (User Prompt):**
    > 進行配課時，加入以科任教師為主的配課模式，選擇教師-->科目-->班級(列出有該科目的班級批次勾選)

*   **實作策略與動機 (Implementation Strategy):**
    *   在【線上互動配課】頁面頂部新增「按班級配課」與「按科任教師批次配課」切換按鈕。
    *   設計科任教師批次配課介面（`#view-assign-by-teacher`）：
        *   第一步：選擇目標教師 (`select-assign-teacher`)，下拉選項顯示該教師目前累計與基本節數。
        *   第二步：選擇授課科目 (`select-assign-teacher-subject`)，動態標示與該教師專長相符的科目 (⭐)。
        *   第三步：動態列出全校所有在課程計畫中設有該科目的班級清單，提供即時核取方框 (Checkbox) 進行批次勾選與指派，並附帶「全選」與「取消全選」按鈕。
    *   雙向狀態同步：在「按科任教師批次配課」下修改的任何指派，與原「依班級配課」矩陣及右側教師即時負擔完全共用 `state.assignments`，資料即時連動。

*   **受影響檔案與變更 (Files Affected & Actions):**
    *   `[MODIFIED]` `index.html`: 新增配課模式分頁按鈕群組及科任教師批次配課矩陣面板。
    *   `[MODIFIED]` `app.js`: 新增 `setAssignMode`, `renderTeacherBatchAssignments`, `batchAssignSelectAll` 函式與相關事件監聽器。
    *   `[MODIFIED]` `dev_history.log`: 記錄本次變更歷程。

*   **目前狀態 (Current Status):** 已完成 Completed

## [2026-07-28 22:53:00] - 清空 data.js 預設初始資料

*   **使用者提示詞 / 需求 (User Prompt):**
    > data.js內有許多預設的內容，請清空，不需要有任何預設的內容

*   **實作策略與動機 (Implementation Strategy):**
    *   將 `data.js` 中的預設班級 (`DEFAULT_CLASSES`)、預設教室 (`DEFAULT_ROOMS`)、預設科目 (`DEFAULT_SUBJECTS`) 與預設教師 (`DEFAULT_TEACHERS`) 全部調整為空陣列/空物件。
    *   所有系統資料由使用者手動新增或透過 CSV 檔案匯入後儲存於瀏覽器 `localStorage`，維持初始狀態完全乾淨。

*   **受影響檔案與變更 (Files Affected & Actions):**
    *   `[MODIFIED]` `data.js`: 清空 `DEFAULT_CLASSES`、`DEFAULT_ROOMS`、`DEFAULT_SUBJECTS`、`DEFAULT_TEACHERS` 的預設陣列與物件內容。
    *   `[MODIFIED]` `dev_history.log`: 記錄本次變更歷程。

*   **目前狀態 (Current Status):** 已完成 Completed

## [2026-07-28 22:52:00] - 移除硬編碼預設科目：導師配課完全依據科目 isHomeroomMain 屬性

*   **使用者提示詞 / 需求 (User Prompt):**
    > 導師配課的資料一律使用科目中的屬性，不要有預設科目

*   **實作策略與動機 (Implementation Strategy):**
    *   移除 `data.js` 與 `app.js` 中原本所有的 `defaultCore`（硬編碼國語/數學/生活/社會等字串陣列）回退判斷。
    *   在 `DEFAULT_SUBJECTS` 預設科目資料集中，明確寫入各科目的 `isHomeroomMain` 布林值屬性（核心科目為 `true`，專科/體育/藝能為 `false`）。
    *   重構 `parseSubjectCSV()`：解析 CSV 時直接依據 CSV 的「導師主要科目」欄位值賦予 `isHomeroomMain` 屬性，不使用任何硬編碼科目名稱進行後備判斷。
    *   重構 `renderClassAssignments()`、`autoAssignHomeroomTeachers()` 與 `renderSubjects()`：統一以 `Boolean(s.isHomeroomMain)` 作為唯一判斷依據，實現真正的「資料驅動」導師主科自訂配課。

*   **受影響檔案與變更 (Files Affected & Actions):**
    *   `[MODIFIED]` `data.js`: 為 `DEFAULT_SUBJECTS` 新增 `isHomeroomMain` 屬性，並移除 `parseSubjectCSV` 中的 `defaultCore` 陣列。
    *   `[MODIFIED]` `app.js`: 移除所有 `defaultCore` 回退判斷，統一讀取 `Boolean(subject.isHomeroomMain)`。
    *   `[MODIFIED]` `dev_history.log`: 記錄本次變更歷程。

*   **目前狀態 (Current Status):** 已完成 Completed

## [2026-07-28 22:50:00] - 優化線上互動配課：讀取班級設定導師與動態科目授課屬性

*   **使用者提示詞 / 需求 (User Prompt):**
    > 線上互動配課中，導師主科綁定應該要讀取班級設定中的導師以及科目授課屬性

*   **實作策略與動機 (Implementation Strategy):**
    *   新增 `getHomeroomTeacherForClass(c)` 通用輔助函式，整合雙向查詢邏輯：同時讀取教師的 `targetClassId` 以及班級設定中的 `homeroomTeacherId` / `teacherId` / `teacher` 欄位。
    *   更新 `renderClassAssignments()`：在【線上互動配課】矩陣中，自動讀取目前班級的導師與各科目的 `isHomeroomMain` 授課屬性。若該科目為導師主科，將班級導師優先置頂於選單，並在科目旁標示 `[導師主科]` 徽章與準確判定「專長相符」。
    *   更新 `autoAssignHomeroomTeachers()`：執行「導師主科一鍵綁定」時，全面透過 `getHomeroomTeacherForClass` 與 `isHomeroomMain` 動態綁定導師主科。

*   **受影響檔案與變更 (Files Affected & Actions):**
    *   `[MODIFIED]` `app.js`: 新增 `getHomeroomTeacherForClass` 輔助函式，重構 `renderClassAssignments` 與 `autoAssignHomeroomTeachers` 綁定及專長判定邏輯。
    *   `[MODIFIED]` `dev_history.log`: 記錄本次變更歷程。

*   **目前狀態 (Current Status):** 已完成 Completed

## [2026-07-28 22:42:00] - 新增「指定導師主要科目」自訂功能與一鍵切換屬性

*   **使用者提示詞 / 需求 (User Prompt):**
    > 在科目項目中，增加指定導師主要科目功能

*   **實作策略與動機 (Implementation Strategy):**
    *   在科目資料結構中擴充 `isHomeroomMain` 屬性（預設一般國語、數學、生活等基礎核心科目為 `true`，專科/體育/藝能科目為 `false`）。
    *   在「科目設定」表格中增加「授課屬性」欄位，並提供可直接點擊切換的 `★ 導師主要科目` / `科任/專科科目` 徽章按鈕，方便即時動態調整。
    *   在「手動新增科目」對話框中加入 `[x] 指定為導師主要科目` 核取方塊。
    *   在 CSV 匯入與範本下載支援新增 `導師主要科目` 欄位（解析 `是`/`否`），並更新 `科目匯入範本.csv`。
    *   更新 `autoAssignHomeroomTeachers()` 邏輯，當執行「導師主科一鍵綁定」時，直接依據科目設定的 `isHomeroomMain` 屬性動態綁定班級導師。

*   **受影響檔案與變更 (Files Affected & Actions):**
    *   `[MODIFIED]` `data.js`: 更新 `parseSubjectCSV` 支援 `導師主要科目` 欄位解析與預設定義。
    *   `[MODIFIED]` `index.html`: 科目表格新增授課屬性標題、Modal 新增核取方塊與匯入預覽欄位。
    *   `[MODIFIED]` `app.js`: 調整 `renderSubjects` 徽章點擊切換、`handleSubjectFormSubmit` 表單處理、`autoAssignHomeroomTeachers` 依據屬性自動綁定。
    *   `[MODIFIED]` `科目匯入範本.csv`: 增加 `導師主要科目` 欄位示範。
    *   `[MODIFIED]` `dev_history.log`: 記錄本次變更歷程。

*   **目前狀態 (Current Status):** 已完成 Completed

## [2026-07-28 22:39:00] - 修復編輯教師視窗「不可排課時段」網格排列錯位問題

*   **使用者提示詞 / 需求 (User Prompt):**
    > 編輯教師不可排課時段視窗排列有誤

*   **實作策略與動機 (Implementation Strategy):**
    *   分析發現 HTML 容器使用的是 `.busy-slots-selector` 類別與 `#busy-slots-grid` ID，但 `style.css` 中原本僅定義了 `.busy-slots-grid` 選擇器，導致 Grid 網格佈局樣式未能正確套用至元素上，造成 48 個時段按鈕與標籤直排錯位。
    *   在 `style.css` 中補充 `.busy-slots-selector` 與 `#busy-slots-grid` 選擇器，設定 `display: grid; grid-template-columns: 60px repeat(5, 1fr);` 佈局與按鈕懸浮/選取樣式（紅底與 ✕ 標示），恢復清晰直觀的週一至週五 1-7 節不排課點選矩陣。

*   **受影響檔案與變更 (Files Affected & Actions):**
    *   `[MODIFIED]` `style.css`: 更新並完善不可排課時段 Selector 網格 CSS 樣式。
    *   `[MODIFIED]` `dev_history.log`: 記錄本次變更歷程。

*   **目前狀態 (Current Status):** 已完成 Completed

## [2026-07-28 22:37:00] - 修正教師 CSV 匯入時姓名欄位誤判為「教師編號」之解析問題

*   **使用者提示詞 / 需求 (User Prompt):**
    > 匯入教師完整名單CSV後，姓名解析錯誤

*   **實作策略與動機 (Implementation Strategy):**
    *   分析發現 `parseTeacherCSV` 於比對表頭 `nameCol` 時，若表頭第一欄包含「教師編號」，其內部的 `includes("教師")` 關鍵字會誤將第 0 欄（教師編號）判定為姓名欄位，導致教師姓名被錯置為「T001」等編號。
    *   在 `data.js` 中優化 `nameCol` 的正則與表頭比對邏輯，排除包含「編號」、「id」、「代碼」等欄位，確保精準抓取「姓名」欄位資料。

*   **受影響檔案與變更 (Files Affected & Actions):**
    *   `[MODIFIED]` `data.js`: 修復 `parseTeacherCSV` 中姓名欄位 (`nameCol`) 的標題過濾邏輯。
    *   `[MODIFIED]` `dev_history.log`: 記錄本次變更歷程。

*   **目前狀態 (Current Status):** 已完成 Completed

## [2026-07-28 22:12:00] - 實作科目 CSV 匯入時自動擷取建立專科教室並移除預設教室

*   **使用者提示詞 / 需求 (User Prompt):**
    > 匯入科目時會連帶匯入需要特殊教室的欄位，專科教室直接取用匯入的資料，不要有預設的教室內容，但保留手動新增的功能

*   **實作策略與動機 (Implementation Strategy):**
    *   移除系統預設預載的專科教室內容 (`DEFAULT_ROOMS` 改為空物件 `{}`)，確保初始環境完全乾淨。
    *   在 `app.js` 中新增 `ensureRoomExists(roomKey)` 輔助函式，當使用者匯入科目 CSV 或手動新增科目時，自動從「特殊教室」欄位擷取教室名稱，並為其自動建立專科教室條目 (預設同時段容量上限為 1 班)。
    *   保留「新增專科教室」按鈕與 Modal，讓使用者後續仍可自由手動新增、編輯容納上限或刪除教室。

*   **受影響檔案與變更 (Files Affected & Actions):**
    *   `[MODIFIED]` `data.js`: 將 `DEFAULT_ROOMS` 調整為空物件 `{}`。
    *   `[MODIFIED]` `app.js`: 新增 `ensureRoomExists` 邏輯，於科目 CSV 匯入與手動科目建立時自動擷取擴充專科教室，並更新空白表格提示訊息。
    *   `[MODIFIED]` `dev_history.log`: 記錄本次變更歷程。

*   **目前狀態 (Current Status):** 已完成 Completed

## [2026-07-28 20:25:00] - 實作專科教室自訂管理（新增、編輯、刪除與動態綁定）

*   **使用者提示詞 / 需求 (User Prompt):**
    > 專科教室也要讓使用者自訂

*   **實作策略與動機 (Implementation Strategy):**
    *   將專科教室資料納入 `state.rooms` 的持久化管理中，使其可透過 JSON 匯出/匯入。
    *   在 `index.html` 中為「專科教室與容納上限」區塊新增「新增專科教室」按鈕與彈出視窗對話框 (`#modal-room`)，支援自訂教室代碼、全名與同時段班級容量上限。
    *   在 `app.js` 中實作專科教室的表格動態渲染、手動新增、編輯上限、刪除教室（並連動清除相關科目的專科教室綁定）之處理邏輯。
    *   動態關聯科目設定 Modal (`#subject-room`) 的下拉選單，使其與 `state.rooms` 的內容同步。
    *   重構 `scheduler.js` 的排課演算法與手動拖曳驗證常式，改為動態讀取傳入的 `state.rooms` 教室限制進行衝突推演。

*   **受影響檔案與變更 (Files Affected & Actions):**
    *   `[MODIFIED]` `data.js`: 新增 `DEFAULT_ROOMS`，並將 `rooms` 納入初始狀態。
    *   `[MODIFIED]` `scheduler.js`: 支援傳入自訂 `rooms` 物件進行排課約束與拖拉移動校驗。
    *   `[MODIFIED]` `index.html`: 新增專科教室新增按鈕、表格欄位與專科教室彈出視窗對話框。
    *   `[MODIFIED]` `app.js`: 實作專科教室增刪改邏輯，動態更新科目選單，並在排課引擎中傳遞 `state.rooms`。
    *   `[MODIFIED]` `dev_history.log`: 記錄本次變更歷程。

*   **目前狀態 (Current Status):** 已完成 Completed

## [2026-07-28 20:11:00] - 移除預設示範資料並強化 JSON 紀錄匯出/匯入功能

*   **使用者提示詞 / 需求 (User Prompt):**
    > 系統中都不要有任何的班級、教師、科目資料，資料都站存在瀏覽器中，加入可以把瀏覽器中的紀錄已josn檔案匯出

*   **實作策略與動機 (Implementation Strategy):**
    *   在 `data.js` 中重構 `getInitialState()`，將 `teachers`, `classes`, `subjects`, `assignments` 全數初始化為空陣列 `[]`，移除預設的示範資料。
    *   在 `app.js` 中調整系統初始化邏輯，使新使用者開啟系統時從零開始，所有資料完全由使用者自行手動建立或透過 CSV/JSON 匯入，並完全暫存於瀏覽器的 `localStorage` 中。
    *   在 `index.html` 的頂部控制列顯著標示「匯出 JSON 紀錄」與「匯入 JSON 紀錄」按鈕，並簡化清空/重置按鈕邏輯。
    *   在 `app.js` 中重構 `exportSystemData()` 函式，支援將 `localStorage` 中的全校教師、班級、科目設定、配課與排課結果完整備份匯出為 JSON 檔案。

*   **受影響檔案與變更 (Files Affected & Actions):**
    *   `[MODIFIED]` `data.js`: 將 `getInitialState` 改為純淨空資料狀態。
    *   `[MODIFIED]` `index.html`: 更新頂部控制列按鈕標示，突顯 JSON 匯出/匯入。
    *   `[MODIFIED]` `app.js`: 調整載入與匯出 JSON 邏輯，補齊空資料狀態下的 UI 提示。
    *   `[MODIFIED]` `dev_history.log`: 記錄本次變更歷程。

*   **目前狀態 (Current Status):** 已完成 Completed

## [2026-07-28 20:04:00] - [Self-Correction / Bugfix] 修正 app.js 重複宣告 targetHours 語法錯誤

*   **使用者提示詞 / 需求 (User Prompt):**
    > 發佈到git pages後，左側功能區無法使用 / app.js:1208 Uncaught SyntaxError: Identifier 'targetHours' has already been declared

*   **實作策略與動機 (Implementation Strategy):**
    *   在 `app.js` 的 `renderClassAssignments` 函式中，發現兩處重複宣告了 `const targetHours`（第 1091 行與第 1208 行），導致瀏覽器在載入腳本時報錯 `Uncaught SyntaxError: Identifier 'targetHours' has already been declared` 並中斷整個 `app.js` 模組的解析與執行，從而造成頁面所有 JS 事件與左側選單完全無反應。
    *   將第 1091 行改為 `const classTargetHours`，並將第 1208 行改為 `const overallTargetHours`，徹底消除變更作用域重複宣告的 SyntaxError。
    *   增加 `safeCreateIcons()` 安全調用封裝，避免 network 異常導致 CDN lucide script 載入失敗時丟出 Uncaught ReferenceError。
    *   強化 `loadAppState()` 狀態還原防護，避免 LocalStorage 的舊資料缺少 `teachers` 或 `classes` 欄位引發執行階段錯誤。

*   **受影響檔案與變更 (Files Affected & Actions):**
    *   `[MODIFIED]` `app.js`: 重新命名 `renderClassAssignments` 內部變數以修正 SyntaxError，並增強 LocalStorage 讀取與 Lucide 圖示調用防護。
    *   `[MODIFIED]` `dev_history.log`: 記錄本次 Bug 修正歷程。

*   **目前狀態 (Current Status):** 已完成 Completed

## [2026-07-28 17:42:00] - 實作動態科目設定管理與配課模組重構

*   **使用者提示詞 / 需求 (User Prompt):**
    > 加入科目設定功能，系統的科目由使用者自行匯入，並且可以指定是哪個年級

*   **實作策略與動機 (Implementation Strategy):**
    *   在 `data.js` 中新增 `parseSubjectCSV(csvText)` 支援年級、科目、每週節數及教室的欄位解析。
    *   在 `data.js` 中重構 `getInitialState()`：將寫死的 `SUBJECTS_MATRIX` 改為扁平化陣列並存入 `state.subjects` 中作為預設基礎資料。
    *   在 `data.js` 中重構 `validateAssignments`，計算全校各班級在 `state.subjects` 中所定義該年級的授課節數總和，使總授課節數指標完全動態化。
    *   在 `index.html` 中新增「科目設定管理」子分頁與「手動新增科目」、「匯入科目 CSV」按鈕、對應 Modal 及 CSV 預覽 Modal。
    *   在 `app.js` 中重構「線上配課」、「一鍵自動配課」與「課表初始化」功能，使其全面讀取 `state.subjects` 的資料進行指派。
    *   在 `app.js` 中實作手動新增科目、刪除科目與匯入科目 CSV 功能。

*   **受影響檔案與變更 (Files Affected & Actions):**
    *   `[MODIFIED]` `data.js`: 重構預設狀態架構與節數驗證函式，新增 `parseSubjectCSV`。
    *   `[MODIFIED]` `index.html`: 新增科目管理 UI 區塊與彈出視窗。
    *   `[MODIFIED]` `app.js`: 重構配課指派演算法與 UI 連動，實作科目 CSV 匯入與增刪處理器。

*   **目前狀態 (Current Status):** 已完成 Completed

## [2026-07-28 17:40:00] - 將 CSV 範本下載功能自拖曳區抽離為獨立區塊

*   **使用者提示詞 / 需求 (User Prompt):**
    > 下載範本檔案的功能要獨立

*   **實作策略與動機 (Implementation Strategy):**
    *   在 `index.html` 中，將「下載範本格式 CSV」從拖曳上傳區 (`#csv-drop-zone`) 的 DOM 結構內部移出，並改在外部新增一個獨立的「下載 CSV 匯入範本」控制區塊，提供：
        1. 教師名單極簡範本 (姓名、職位)
        2. 教師名單完整範本 (含專長等)
        3. 班級清單範本
    *   在 `app.js` 中重構與綁定這些獨立範本下載按鈕的事件處理常式。

*   **受影響檔案與變更 (Files Affected & Actions):**
    *   `[MODIFIED]` `index.html`: 重構教師與班級設定視圖，新增獨立的範本下載按鈕組，並從拖曳區中移除下載連結。
    *   `[MODIFIED]` `app.js`: 新增並重構 `downloadClassCSVTemplate` 及 `downloadTeacherMinimalCSVTemplate` 的下載觸發邏輯。

*   **目前狀態 (Current Status):** 已完成 Completed

## [2026-07-28 17:39:00] - [Self-Correction / Bugfix] 修正 CSV 範本下載連結冒泡觸發檔案選擇問題

*   **使用者提示詞 / 需求 (User Prompt):**
    > 點選下載範本格式CSV會直接啟動匯入檔案選擇，而不是下載。

*   **實作策略與動機 (Implementation Strategy):**
    *   在 `app.js` 的 `#download-csv-template` 點擊事件處理常式中加入 `e.stopPropagation()`，防止點擊事件冒泡至父元素 `#csv-drop-zone` 導致重複觸發隱藏的 file input 檔案選擇視窗。

*   **受影響檔案與變更 (Files Affected & Actions):**
    *   `[MODIFIED]` `app.js`: 於下載範本連結事件中加入 `e.stopPropagation()`。

*   **目前狀態 (Current Status):** 已完成 Completed

## [2026-07-28 17:38:00] - 建立 CSV 匯入範本檔案

*   **使用者提示詞 / 需求 (User Prompt):**
    > 提供匯入CSV範本檔

*   **實作策略與動機 (Implementation Strategy):**
    *   在專案根目錄中建立「教師匯入範本_極簡.csv」、「教師匯入範本_完整.csv」以及「班級匯入範本.csv」，供使用者手動測試匯入流程。

*   **受影響檔案與變更 (Files Affected & Actions):**
    *   `[CREATED]` `教師匯入範本_極簡.csv`: 簡易版教師 CSV 範本。
    *   `[CREATED]` `教師匯入範本_完整.csv`: 完整版教師 CSV 範本。
    *   `[CREATED]` `班級匯入範本.csv`: 班級清單 CSV 範本。

*   **目前狀態 (Current Status):** 已完成 Completed

## [2026-07-28 17:35:00] - 新增「班級 CSV 匯入」與「智慧相容教師清單 CSV 匯入」功能

*   **使用者提示詞 / 需求 (User Prompt):**
    > 加入可以匯入班級、教師清單（姓名、職位）

*   **實作策略與動機 (Implementation Strategy):**
    *   在 `data.js` 中實作 `parseClassCSV(csvText)`，支援多種欄位排列與自動偵測標頭（含年級自動推導），將解析後的班級匯入系統並自動初始化全校配課項目。
    *   重構 `parseTeacherCSV(csvText)` 使其具備智慧型相容模式：自動識別是否有標頭列。若有標頭，依關鍵字定位；若無標頭且格式簡化（例如只有姓名與職位），自動按欄位數對齊並循序生成教師 ID（T001...）。
    *   在 `index.html` 中為班級列表新增「匯入班級 CSV」按鈕，並設計確認預覽 Modal。
    *   在 `app.js` 中實現匯入班級 CSV 的處理常式與 UI 事件綁定。

*   **受影響檔案與變更 (Files Affected & Actions):**
    *   `[MODIFIED]` `data.js`: 新增 `parseClassCSV` 並重構 `parseTeacherCSV` 以相容簡化格式與無標頭 CSV。
    *   `[MODIFIED]` `index.html`: 新增班級 CSV 檔案選擇與上傳按鈕，並為班級匯入設計預覽對話框。
    *   `[MODIFIED]` `app.js`: 綁定班級 CSV 匯入按鈕與預覽確認邏輯。

*   **目前狀態 (Current Status):** 已完成 Completed

## [2026-07-28 17:28:00] - 新增「清空所有資料」與「手動新增/刪除班級」功能

*   **使用者提示詞 / 需求 (User Prompt):**
    > 加入清空所有資料：班級、教師，後續由使用者建立

*   **實作策略與動機 (Implementation Strategy):**
    *   在 `index.html` 中新增「清空所有資料」按鈕，以及「手動新增班級」的 Modal 對話框。
    *   在 `app.js` 中實作「清空所有資料」功能，會重設教師、班級、配課、排課表為空值，並存檔。
    *   在 `app.js` 中實作手動新增班級功能，新增班級時自動載入對應年級的課務預設配課（但教師設為未指派），供後續進行線上配課。
    *   在 `app.js` 的班級卡片上新增「刪除」按鈕，讓使用者可以單獨移除特定班級。

*   **受影響檔案與變更 (Files Affected & Actions):**
    *   `[MODIFIED]` `index.html`: 新增清空按鈕與新增班級對話框。
    *   `[MODIFIED]` `app.js`: 新增清空資料、新增班級、刪除班級與對應的 UI 事件綁定。

*   **目前狀態 (Current Status):** 已完成 Completed

## [2026-07-28 17:15:00] - 修正線上配課匹配進度與清空邏輯

*   **使用者提示詞 / 需求 (User Prompt):**
    > 修正「科任專長自動匹配」進度停留在 65% 的問題，並修正「清空配課資料」重設後無法重新完全匹配的瑕疵。

*   **實作策略與動機 (Implementation Strategy):**
    *   在 `data.js` 的 `getInitialState()` 中，將全校 15 個班級的所有科目配課物件在系統初始化時即行建立（預設 `teacherId` 為空字串），避免因懶載入導致部分科目未在 `state.assignments` 中而無法被自動配課匹配。
    *   在 `app.js` 的「清空配課資料」按鈕事件中，將配課陣列之 `teacherId` 設為空字串，而非直接將整個陣列設為空，保留配課架構供重新匹配。

*   **受影響檔案與變更 (Files Affected & Actions):**
    *   `[MODIFIED]` `data.js`: 於 `getInitialState()` 中完整初始化全校所有班級的課務配課項目。
    *   `[MODIFIED]` `app.js`: 調整清空配課資料按鈕邏輯，僅清空教師指派關係。

*   **目前狀態 (Current Status):** 已完成 Completed

## [2026-07-28 17:05:00] - [Self-Correction / Bugfix] 自我修正/錯誤修復

*   **使用者提示詞 / 需求 (User Prompt):**
    > [Self-Correction / Bugfix] 修正 scheduler.js 語法錯誤

*   **實作策略與動機 (Implementation Strategy):**
    *   修正 `scheduler.js` 中 for 迴圈結束處的語法標點符號，將錯誤的 `});` 改為 `}`。

*   **受影響檔案與變更 (Files Affected & Actions):**
    *   `[MODIFIED]` `scheduler.js`: 修正 for 迴圈結尾之 SyntaxError。

*   **目前狀態 (Current Status):** 已完成 Completed

## [2026-07-28 17:00:42] - 系統初始化與開發啟動

*   **使用者提示詞 / 需求 (User Prompt):**
    > 開始分析並且思考如何開發。使用者已核准實作計畫。

*   **實作策略與動機 (Implementation Strategy):**
    *   建立專案的目錄架構，包含：
        *   `dev_history.log`：專案開發歷程日誌。
        *   `index.html`：單頁 Web App 入口與骨架。
        *   `style.css`：質感暗色與亮色模式 CSS 設計系統。
        *   `data.js`：資料結構、測試資料、CSV 解析與 LocalStorage 持久化。
        *   `scheduler.js`：啟發式優先度 + 回溯法排課引擎。
        *   `app.js`：拖拉調課、自動配課、Excel 匯出及 UI 事件主控器。

*   **受影響檔案與變更 (Files Affected & Actions):**
    *   `[CREATED]` `dev_history.log`: 初始化開發歷程日誌檔。
    *   `[CREATED]` `index.html`: 新增排課系統的主網頁結構。
    *   `[CREATED]` `style.css`: 新增設計系統與切換模式樣式。
    *   `[CREATED]` `data.js`: 新增預設資料模型與驗證邏輯。
    *   `[CREATED]` `scheduler.js`: 新增自動排課的核心演算法。
    *   `[CREATED]` `app.js`: 新增 UI 控制與互動邏輯。

*   **目前狀態 (Current Status):** 進行中 In Progress
